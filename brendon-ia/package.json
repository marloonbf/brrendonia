{
  "name": "brendon-ia",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm --prefix client install",
    "build": "npm --prefix client run build",
    "dev": "npm --prefix client run dev"
  }
}