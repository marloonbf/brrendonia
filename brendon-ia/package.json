{
  "name": "brendon-ia",
  "private": true,
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "postinstall": "npm --prefix client install",
    "build": "npm --prefix client run build",
    "dev": "npm --prefix client run dev"
  }
}
