{
  "name": "brendon-ia",
  "version": "1.0.0",
  "private": true,
  "description": "Brendon IA - API + Client",
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  }
}
